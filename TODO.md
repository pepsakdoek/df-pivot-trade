# TODO

* Make the 'stocks' screen work (you can't select it (or maybe you can - but only to do the 'stock' actions with it)
    * Stock actions: Move the game cursor to that item (only available when one item is selected / maybe default to the top item based on sort?)
    * View the item: Open the item's sheet (only available when one item is selected / maybe default to the top item based on sort?)
    * Toggle forbidden status
    * Toggle dump status
    * Toggle melt status
    * Toggle item visibility
    * Allow the stocks screen to also (if a trader is there): move it to the trade depo
